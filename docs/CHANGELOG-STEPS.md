# BARFF.UZ — STEP LOG

One line per completed ROADMAP step. Append only; never rewrite history here.

Format:

```
<step> | <YYYY-MM-DD> | <short description> | <PR> | notes: <anything worth knowing>
```

---

S00 | 2026-08-27 | bootstrap monorepo | — | notes: pnpm workspace + turborepo, eslint 9 flat config, prettier, strict tsconfig base with @barff/* aliases, .env.example (names only), docker-compose with postgres/redis/minio + bucket init, docs templates. apps/*, services/api, packages/*, prisma, infrastructure are .gitkeep placeholders — filled by S01-S06. Source assets extracted to assets/ (see ASSETS.md). docker compose validated with `config`; `up` not runnable in the build container.
S01 | 2026-08-27 | shared packages skeleton | — | notes: @barff/config (eslint+tsconfig+tailwind presets, §16 design tokens with placeholder brand ramp per Q-011), @barff/types (as-const enums for roles/order/delivery/lead/movement + locale, pagination, api error), @barff/utils (integer-minor-unit money with exact allocation, Tashkent-time dates, Cyrillic-aware slugs, formatting), @barff/validation (zod 4, depends on types+utils). 109 unit tests pass. Root eslint config and tsconfig.base.json now consume @barff/config. DoD "importable from an app" only partly verifiable — no app exists until S06; proven instead via cross-package imports (validation -> types + utils) and root consumption of @barff/config.
S02 | 2026-08-27 | nestjs api skeleton | — | notes: services/api on Nest 11 under /api/v1. Zod env validation (fail fast, all problems at once), AppConfigService as the only env reader, AsyncLocalStorage request ids echoed on x-request-id, custom JSON logger with central secret redaction (no pino), AllExceptionsFilter emitting @barff/types' ApiError shape, terminus health with real SELECT 1 / PING split into live vs ready, Swagger at /api/v1/docs (off in production), throttler + helmet + strict CORS (no wildcard), pagination/sort DTO helpers with column allow-list, ZodValidationPipe bridging @barff/validation. 55 tests. Health verified green against a real Postgres 16 + Redis, plus the 503 degraded path and recovery. Two bugs found and fixed during verification: the global filter was flattening terminus payloads (a 503 no longer said which dependency was down), and ConfigModule.forRoot() evaluates at import time so test env overrides need vi.hoisted(). pg is a temporary dependency for the DB probe and leaves at S03 when Prisma arrives.
S03 | 2026-08-27 | prisma core schema and seed | — | notes: 7 models (users, roles, permissions, user_roles, role_permissions, system_settings, audit_logs) at prisma/ per §15, snake_case @@map, unique indexes on users.email and users.phone. Idempotent seed: 6 roles with keys taken from @barff/types, 49-permission resource:action baseline, 81 grants (ADMIN computed from the full set so a new permission is never withheld; every other role explicit), 4 technical settings, admin from SEED_ADMIN_* (skipped with a warning if unset). Argon2id params in one file shared by the seed and S04's verifier — round-trip verified against the seeded hash. PrismaService with graceful shutdown; pg dropped as promised at S02 and the readiness probe now shares the app's pool. Migrated package.json#prisma to prisma.config.ts (deprecated in Prisma 6, removed in 7). Reproducibility proven on a throwaway database (migrate deploy + seed twice, counts unchanged) rather than `migrate reset`, which Prisma blocks for AI agents without explicit user consent. One design fix: PrismaService no longer throws when the database is unreachable at boot, since crashing would turn a brief RDS blip into every ECS task failing to start. 175 tests.
S04 | 2026-08-27 | auth and server-side rbac | — | notes: login/refresh/logout/me with argon2id (params shared with the S03 seed — the seeded admin logs in, verified live). Access token (15m, carries roles+permissions) and refresh token (30d, carries neither) signed with separate secrets; env refuses to start if they match or are under 32 chars. Refresh rotation with reuse detection — replaying a consumed token revokes the whole session family. Only SHA-256 digests in Redis, never the tokens. HttpOnly SameSite=Lax cookies, refresh cookie scoped to /auth/refresh; body tokens only on X-Token-Delivery: body (driver PWA). No user enumeration: identical 401 for unknown/wrong/locked/deactivated, with a dummy hash on the unknown path to level timing. Two-layer throttle (10/min per IP + 5-attempt account lockout). Global JwtAuthGuard = deny by default with @Public() opt-out; RolesGuard/PermissionsGuard refuse without naming the missing grant. Audit rows for login success/failure/blocked, logout, refresh, reuse detection. 133 API tests, 208 workspace-wide. DoD "no token/password ever logged" asserted by a test that runs the app at debug level and greps the whole transcript. Shared RedisService replaces the health indicator's private client. Three pre-existing tests needed fixing — deny-by-default correctly started blocking their probe controllers.
S05 | 2026-08-27 | ci pipeline and docker images | — | notes: services/api/Dockerfile (multi-stage, non-root node uid 1000, Debian slim for argon2/Prisma glibc prebuilds, built from the repo root, exec-form CMD so node is PID 1 and gets SIGTERM). ci.yml runs format/lint/typecheck/test/build against real postgres+redis service containers with migrate+seed applied, plus a second job that builds the image, smoke-tests /health/live and fails if the container runs as root; throwaway JWT secrets generated per run rather than stored. deploy-staging.yml is a guarded skeleton (workflow_dispatch + typed confirmation + preflight that refuses until S40 provisions AWS), OIDC role assumption not static keys, SHA-tagged images, migrations as a one-off ECS task before the service update. docs/BRANCH-PROTECTION.md records the GitHub-side settings. One Next.js Dockerfile template in infrastructure/docker/ rather than four copies for empty app dirs — apps/* do not exist until S06. Two packaging bugs found by replaying the build natively: pnpm deploy --prod dropped the Prisma client (fixed by an explicit generator output inside services/api, which is also where Prisma 7 forces this) and skipped argon2's postinstall (fixed by repeating onlyBuiltDependencies in services/api/package.json) — the image would have crash-looped on "Cannot find module '.prisma/client/default'". docker build itself is NOT verifiable in this container: the egress gateway denies production.cloudfront.docker.com, so no base image can be pulled; CI performs the real build. 208 tests; full CI sequence replayed locally against a fresh database and green.
S06 | 2026-08-27 | web app shell and theme tokens | — | notes: PHASE 0 GATE PASSED. apps/web on Next 15 App Router + React 19 + Tailwind v4, loading the S01 design tokens through @config so the TS tokens stay the single source of truth rather than being duplicated into CSS. next-intl routing /uz|/ru|/en with uz default and localePrefix always (an unprefixed default would give the same content two URLs and cost the canonical); locales come from @barff/types. Header/footer/container/skip-link/locale-switcher, plus loading, error and not-found per locale and a global not-found outside any locale. TanStack Query provider created inside useState (a module-scope client leaks one visitor's cache into another's response); api-client maps failures onto the API's ApiError shape with timeouts and credentials. prefers-reduced-motion handled globally in CSS plus a hook for the JS half S16/S17 need. VERIFIED: Lighthouse accessibility 100/100 on all three locales with zero failing audits (DoD >= 90); no horizontal overflow at 360/390/768/1440; mobile menu aria-expanded toggles; locale switch preserves the route; standalone bundle boots and serves; API health + auth login green; CORS preflight from the web origin allowed. Also perf 99, SEO 100, CLS 0. Two real fixes: footer links were 16px tall, below WCAG 2.2 SC 2.5.8's 24px target minimum which Lighthouse does not check; and my own no-hardcoded-strings test was vacuous — its regex excluded newlines so it missed all multi-line JSX, proven by planting a violation, fixed, and re-proven to fail then pass. middleware.ts had to move to src/middleware.ts because a src directory changes where Next looks. 225 tests.
S07 | 2026-08-28 | barff design system | — | notes: packages/ui with 14 primitives (Button, Link, Badge, Field, Input, Textarea, Checkbox, Select, Dialog, Sheet, Tabs, Accordion, Toast, Skeleton, Pagination) and 5 surfaces (GlassCard, Section, SectionHeader, StatBlock, MediaFrame). Radix for everything interactive — focus traps, focus return, roving tabindex, listbox typeahead and live regions are where hand-rolled design systems fail their a11y promise. Ships TS source, not a bundle, because 'use client' does not survive bundling. Review route at /uz/dev/ui instead of Storybook (real theme, real Tailwind build, auditable; never served in production). No user-facing text in the package — every label is a required prop, enforced by a test. VERIFIED: Lighthouse a11y 100/100 on /dev/ui and /uz with zero failing audits; keyboard suite covering dialog focus trap+return, tab roving tabindex with Home/End, accordion Enter, select listbox focus return, checkbox Space, and a focus ring on every focusable element. THREE REAL BUGS FOUND BY MEASURING: (1) Tailwind never scanned packages/ui — a v4 content glob does not reach a symlinked workspace package — so the ENTIRE design system rendered unstyled while looking plausible; caught because pagination buttons measured 5px wide, fixed with @source in globals.css. (2) danger fill failed contrast at 3.55:1, and one red cannot serve both text-on-dark and fill-under-white, so state.danger and state.dangerFill are now two tokens with both ratios asserted. (3) select/input/textarea placeholders used content.disabled at 2.72:1 — a placeholder is live text, not a disabled control. Also fixed a hard-coded English "(required)" in Field, found by the package's own §18 test, and three bugs in my own constraint checks (a comment read as a class use, /i matching <Button> as <button>, and generics parsed as JSX text). New contrast.test.ts in @barff/config asserts every rendered token pair against WCAG AA. 363 tests workspace-wide.
